name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: [self-hosted, tenma]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t localhost:5000/qr:latest -t localhost:5000/qr:${{ github.sha }} .

      - name: Push to local registry
        run: |
          docker push localhost:5000/qr:latest
          docker push localhost:5000/qr:${{ github.sha }}
